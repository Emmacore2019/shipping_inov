<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        
        <!-- Vues pour shipping.charge.item -->
        <record id="view_shipping_charge_item_tree" model="ir.ui.view">
            <field name="name">shipping.charge.item.tree</field>
            <field name="model">shipping.charge.item</field>
            <field name="arch" type="xml">
                <tree string="Port Charges Tariff Items" editable="bottom">
                    <field name="code"/>
                    <field name="name"/>
                    <field name="category"/>
                    <field name="calc_mode"/>
                    <field name="price_unit" widget="monetary"/>
                    <field name="currency_id" groups="base.group_multi_currency"/>
                    <field name="product_id"/>
                    <field name="active"/>
                </tree>
            </field>
        </record>

        <record id="view_shipping_charge_item_form" model="ir.ui.view">
            <field name="name">shipping.charge.item.form</field>
            <field name="model">shipping.charge.item</field>
            <field name="arch" type="xml">
                <form string="Port Charge Item">
                    <sheet>
                        <div class="oe_button_box" name="button_box">
                            <button name="toggle_active" type="object" 
                                    class="oe_stat_button" icon="fa-archive">
                                <field name="active" widget="boolean_button" 
                                       options='{"terminology": "archive"}'/>
                            </button>
                        </div>
                        <div class="oe_title">
                            <label for="code" class="oe_edit_only"/>
                            <h1><field name="code" placeholder="e.g. 1.1"/></h1>
                        </div>
                        <group>
                            <group>
                                <field name="name"/>
                                <field name="category"/>
                                <field name="calc_mode"/>
                            </group>
                            <group>
                                <field name="price_unit" widget="monetary"/>
                                <field name="currency_id" groups="base.group_multi_currency"/>
                                <field name="product_id" context="{'default_type': 'service'}"/>
                            </group>
                        </group>
                        <group string="Taxes">
                            <field name="tax_ids" nolabel="1"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="view_shipping_charge_item_search" model="ir.ui.view">
            <field name="name">shipping.charge.item.search</field>
            <field name="model">shipping.charge.item</field>
            <field name="arch" type="xml">
                <search string="Search Port Charges Items">
                    <field name="code"/>
                    <field name="name"/>
                    <field name="category"/>
                    <field name="product_id"/>
                    <separator/>
                    <filter string="Active" name="active" domain="[('active','=',True)]"/>
                    <filter string="Archived" name="inactive" domain="[('active','=',False)]"/>
                    <separator/>
                    <group expand="0" string="Group By">
                        <filter string="Category" name="group_by_category" domain="[]" context="{'group_by':'category'}"/>
                        <filter string="Calculation Mode" name="group_by_calc_mode" domain="[]" context="{'group_by':'calc_mode'}"/>
                    </group>
                </search>
            </field>
        </record>

        <!-- Action pour shipping.charge.item -->
        <record id="action_shipping_charge_item" model="ir.actions.act_window">
            <field name="name">Port Charges Tariff</field>
            <field name="res_model">shipping.charge.item</field>
            <field name="view_mode">tree,form</field>
            <field name="search_view_id" ref="view_shipping_charge_item_search"/>
            <field name="context">{'search_default_active': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create a new port charge tariff item
                </p>
                <p>
                    Port charges tariff items are used to configure the standard charges 
                    that apply to shipping operations. Each item can have different calculation modes.
                </p>
            </field>
        </record>

        <!-- Menu -->
        <menuitem 
            name="Port Charges Tariff"
            action="action_shipping_charge_item"
            id="menu_shipping_charge_item" 
            parent="menu_shipping_configuration_id" 
            groups="inov_transit.group_shipping_manager"
            sequence="10"/>

        <!-- Vues pour shipping.charge.line (embedded) -->
        <record id="view_shipping_charge_line_tree" model="ir.ui.view">
            <field name="name">shipping.charge.line.tree</field>
            <field name="model">shipping.charge.line</field>
            <field name="arch" type="xml">
                <tree string="Port Charges" editable="bottom">
                    <field name="code"/>
                    <field name="description"/>
                    <field name="category" invisible="1"/>
                    <field name="calc_mode"/>
                    <field name="qty"/>
                    <field name="price_unit" widget="monetary"/>
                    <field name="amount" widget="monetary" sum="Total"/>
                    <field name="currency_id" invisible="1"/>
                    <field name="item_id" invisible="1"/>
                </tree>
            </field>
        </record>

        <record id="view_shipping_charge_line_form" model="ir.ui.view">
            <field name="name">shipping.charge.line.form</field>
            <field name="model">shipping.charge.line</field>
            <field name="arch" type="xml">
                <form string="Port Charge Line">
                    <sheet>
                        <group>
                            <group>
                                <field name="item_id"/>
                                <field name="category"/>
                                <field name="calc_mode"/>
                            </group>
                            <group>
                                <field name="qty"/>
                                <field name="price_unit" widget="monetary"/>
                                <field name="amount" widget="monetary"/>
                                <field name="currency_id"/>
                            </group>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

    </data>
</odoo>